package homework7.wrapperclasses;

import static java.lang.Integer.parseInt;

/**
 * 	1. Implements wrapper by your self (Integer, Double, Character, Boolean)
 * 				2. Implements at least 3, different methods for each Wrapper Class
 * 						ex. int intValue()
 * 							  int parseInt(String number)
 * 							  void increment()
 * 							  Integer plus(Integer other) // minus
 */
public class WrapTest {
    public static void main(String[] args) {
        byte b = 45;
        String  s = "12";
        Integer.bitCount(45);
        byte b1 = 4;
        System.out.println( Byte.toString(b));
        System.out.println(Byte.valueOf(s));



    }

}
